# Cat-rew_laba-3
ЛАБОРАТОРНАЯ РАБОТА №3. Агафонов Михаил, ИТ-16-2024, вариант 2
GEMINI.
задача 1


Промт: Напиши код для данной задачи на Языке Программирования C++

Ввести с клавиатуры любое слово. Используя генерацию случайных чисел, переставить 
буквы этого слова в случайном порядке. Делать это до тех пор, пока полученное слово не совпадёт 
с начальным словом. Выводить слово после каждой перестановки и посчитать общее количество 
выведенных слов (не считая исходного). Пример выполнения программы: 
Введите слово: корова 
воакро 
3. 
вокроа       
ароовк 
краоов 
крваоо 
оокавр 
ооквра 
вкраоо 
корова 
9 попыток

Вывод: Рабочий код, разделение на модули не прописывалось, код стайл не прописывался.

Реализация:
- Код разделён на модули
- Код оформлен в соответствии с код стайлом от Epic Games
- Строгое соблюдение PascalCase для имен и префикса `b` для логических переменных (`bIsRussian`). Используется префиксный инкремент `++Attempts`.
- Валидация реализована через проверку числовых кодов символов.
- Логика проверки и перемешивания вынесена в отдельные функции. Запрещено использование `using namespace std`, везде используются явные префиксы.
- Вместо `rand()` применен генератор Вихрь Мерсенна (`std::mt19937`), что дает качественное перемешивание при каждом запуске.

ЗАДАЧА 2


Промт:Напиши код для данной задачи на Языке программирования C++

В текстовом файле с именем FN1 дано арифметическое выражение в инфиксной 
форме. В выражении могут использоваться операции: сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), а так же целые числа из про
межутка [1; 30] и переменная x. Для операции возведения в степень показатель степени неотри
цательное целое число. Постройте дерево выражения. После этого вычислите значение выраже
ния при заданном значении переменной x и выведите результат в текстовый файл с именем FN2. 
Преобразуйте дерево, заменив все поддеревья вида A+x на x+A, где A - некоторое поддерево, а x - переменная. Распечатайте дерево после преобразования в файл FN2 в префиксной и постфикс
ной форме, а так же в инфиксной форме с избыточными скобками. При наличии нескольких под
ряд идущих одинаковых операций дерево должно строиться по правилу: операции одинакового 
приоритета вычисляются по порядку слева направо. Иными словами, выражение 2+3+4+5, напри
мер, должно трактоваться как ((2+3)+4)+5, и не может трактоваться как (2+3)+(4+5) или 
2+(3+(4+5)). Результаты всех вычислений, включая промежуточные, принадлежат типу int.

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных (Данные читаются файла, на вход принимаются только целые числа, сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), переменная x

Вывод: Рабочий код, разделение на модули выполнено, соблюден Code Style от Epic Games.

Реализация:

- Код разделён на модули
- Код оформлен в соответствии с Code Style от Epic Games
- Валидация данных
    - Файловая: При чтении из `FN1.txt` проверяется корректность символов и диапазон чисел [1; 30]. В случае некорректного выражения программа выводит ошибку и завершается.
    - Консольная: Ввод переменной x защищен от букв и спецсимволов. Программа считывает строку целиком и проверяет каждый символ, прежде чем преобразовать его в число.
    - Алгоритм построения: Реализован алгоритм сортировочной станции Эдсгера Дейкстры для преобразования инфиксной записи в постфиксную с учетом приоритетов и левой ассоциативности, после чего строится бинарное дерево.
- Трансформация: Рекурсивная функция ищет узлы с оператором `+`, проверяет правый операнд на принадлежность к типу `Variable` и выполняет перестановку указателей на поддеревья.

ЗАДАЧА 3
Промт: Напиши код для данной задачи на языке программирования на C++

Дано описание неориентированного графа в текстовом файле с именем FileName1. в виде 
матрицы смежности. Первая строка файла содержит количество вершин графа (n), а следующие 
n строк содержат матрицу смежности (a), a[i][j]=0, если ребра между вершинами i и j не суще
ствует. Построить матрицу инцидентности данного графа и вывести ее в файл с именем 
FileName2. Для справки: матрица инцидентности (b) имеет размер n x m, m - число ребер графа, 
b[i][j]=1, если ребро j инцидентно вершине i, в противном случае b[i][j]=0. Нумерацию ребер осу
ществлять в следующем порядке: сначала ребра, инцидентные вершине номер 1, потом ребра ин
цидентные вершине номер 2 и т.д. до вершины номер n. Ребра, инцидентные вершине с номером 
i перечислять в порядке возрастания номера второй вершины, инцидентной данному ребру. При 
выводе в первой строке указать размер матрицы инцидентности: числа n и m, а в следующих n 
строках разместить матрицу инцидентности.

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных

Вывод: Сгенерировался код с первого раза

Реализация:

Код разделён на модули: `GraphConverter.h` — содержит определение класса `FGraphConverter` и структуры данных.
    - `GraphConverter.cpp` — логика обработки матрицы смежности и генерации матрицы инцидентности.
    - `Main.cpp` — точка входа, работа с файловыми потоками и консольный вывод.
Код оформлен в соответствии с Code Style от Epic Games:
    - Использование PascalCase для именования функций (`BuildIncidenceMatrix`) и типов данных.
    - Префиксы для классов (`F`) и логических переменных (`b`).
    - Чистый код без использования `using namespace std`.
Алгоритм построения:
    - Программа считывает количество вершин $n$.
    - Выполняется проход по верхней треугольной матрице смежности для точного подсчета ребер $m$ и их сортировки согласно условию (по возрастанию номеров вершин).
    - Матрица инцидентности заполняется значениями `0` и `1` на основе найденных связей.
Валидация данных:
    - Проверка успешного открытия файлов `FileName1` и `FileName2`.
    - Валидация корректности размера матрицы (число вершин n должно быть положительным).
Память и производительность:
    - Используется `std::vector<std::vector<int32>>` для динамического управления памятью матриц.
    - Префиксный инкремент (`++i`) в циклах для оптимизации.

ОБЩИЙ вывод по нейронке: Gemini показал себя хорошо. При хорошем и подробном промте рабочий код может получиться с первого раза.

СhatGPT
ЗАДАЧА 1

Промт:

Напиши код для данной задачи на Языке Программирования C++

Ввести с клавиатуры любое слово. Используя генерацию случайных чисел, переставить 
буквы этого слова в случайном порядке. Делать это до тех пор, пока полученное слово не совпадёт 
с начальным словом. Выводить слово после каждой перестановки и посчитать общее количество 
выведенных слов (не считая исходного). Пример выполнения программы: 
Введите слово: корова 
воакро 
3. 
вокроа       
ароовк 
краоов 
крваоо 
оокавр 
ооквра 
вкраоо 
корова 
9 попыток

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных (на вход должны приниматься только английские и русские буквы)

Вывод: Рабочий код. Что пришлось добавить и улучшить: валидацию данных (код не считывал кириллицу), коммиты (отсуствовали полностью).

ЗАДАЧА 2

Промт:

Напиши код для данной задачи на Языке программирования C++

В текстовом файле с именем FN1 дано арифметическое выражение в инфиксной 
форме. В выражении могут использоваться операции: сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), а так же целые числа из про
межутка [1; 30] и переменная x. Для операции возведения в степень показатель степени неотри
цательное целое число. Постройте дерево выражения. После этого вычислите значение выраже
ния при заданном значении переменной x и выведите результат в текстовый файл с именем FN2. 
Преобразуйте дерево, заменив все поддеревья вида A+x на x+A, где A - некоторое поддерево, а x - переменная. Распечатайте дерево после преобразования в файл FN2 в префиксной и постфикс
ной форме, а так же в инфиксной форме с избыточными скобками. При наличии нескольких под
ряд идущих одинаковых операций дерево должно строиться по правилу: операции одинакового 
приоритета вычисляются по порядку слева направо. Иными словами, выражение 2+3+4+5, напри
мер, должно трактоваться как ((2+3)+4)+5, и не может трактоваться как (2+3)+(4+5) или 
2+(3+(4+5)). Результаты всех вычислений, включая промежуточные, принадлежат типу int.

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных (Данные читаются файла, на вход принимаются только целые числа, сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), переменная x

Вывод: Рабочий код. Что пришлось добавить и улучшить: валидацию данных (код не корректно обрабатывал целые числа в файле). Коммиты по прежнему отсутствуют.

ЗАДАЧА 3

Промт: Напиши код для данной задачи на языке программирования на C++

Дано описание неориентированного графа в текстовом файле с именем FileName1. в виде 
матрицы смежности. Первая строка файла содержит количество вершин графа (n), а следующие 
n строк содержат матрицу смежности (a), a[i][j]=0, если ребра между вершинами i и j не суще
ствует. Построить матрицу инцидентности данного графа и вывести ее в файл с именем 
FileName2. Для справки: матрица инцидентности (b) имеет размер n x m, m - число ребер графа, 
b[i][j]=1, если ребро j инцидентно вершине i, в противном случае b[i][j]=0. Нумерацию ребер осу
ществлять в следующем порядке: сначала ребра, инцидентные вершине номер 1, потом ребра ин
цидентные вершине номер 2 и т.д. до вершины номер n. Ребра, инцидентные вершине с номером 
i перечислять в порядке возрастания номера второй вершины, инцидентной данному ребру. При 
выводе в первой строке указать размер матрицы инцидентности: числа n и m, а в следующих n 
строках разместить матрицу инцидентности.

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных
3) Добавь комментарии на русском языке

Вывод: Рабочий код. Код стайл нуждаеся в существенной доработке. Коммиты появились, но только после промта.

ОБЩИЙ вывод по ChatGPT. НОвые модели на ограниченное количество запросов справляются хорошо, но встречались надоработки в валидации данных. Беплатные версии справлялись очень плохо: не соблюдался код стайл, валидация некорректна.

CLAUDE
ЗАДАЧА 1

Промт:
Напиши код для данной задачи на Языке Программирования C++ 
Ввести с клавиатуры любое слово. Используя генерацию случайных чисел, переставить буквы этого слова в случайном порядке. Делать это до тех пор, пока полученное слово не совпадёт с начальным словом. Выводить слово после каждой перестановки и посчитать общее количество выведенных слов (не считая исходного). Пример выполнения программы: Введите слово: корова воакро 3. вокроа ароовк краоов крваоо оокавр ооквра вкраоо корова 9 попыток
код стайл должен быть от epic games на входе должны приниматься только русские и английские буквы

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных (на вход принимаются только русские и английские буквы)

Вывод: Рабочий код. Соотвествует код стайлу от epic games, однако все комментарии генерируются на английском языке.

Задача 2
Промт:

Напиши код для данной задачи на Языке программирования C++

В текстовом файле с именем FN1 дано арифметическое выражение в инфиксной 
форме. В выражении могут использоваться операции: сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), а так же целые числа из про
межутка [1; 30] и переменная x. Для операции возведения в степень показатель степени неотри
цательное целое число. Постройте дерево выражения. После этого вычислите значение выраже
ния при заданном значении переменной x и выведите результат в текстовый файл с именем FN2. 
Преобразуйте дерево, заменив все поддеревья вида A+x на x+A, где A - некоторое поддерево, а x - переменная. Распечатайте дерево после преобразования в файл FN2 в префиксной и постфикс
ной форме, а так же в инфиксной форме с избыточными скобками. При наличии нескольких под
ряд идущих одинаковых операций дерево должно строиться по правилу: операции одинакового 
приоритета вычисляются по порядку слева направо. Иными словами, выражение 2+3+4+5, напри
мер, должно трактоваться как ((2+3)+4)+5, и не может трактоваться как (2+3)+(4+5) или 
2+(3+(4+5)). Результаты всех вычислений, включая промежуточные, принадлежат типу int.
Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных (Данные читаются файла, на вход принимаются только целые числа, сложение(+), вычитание(-), умножение(*), 
деление нацело(/), остаток от деления(%), возведение в степень(^), переменная x

Вывод: Раюочий код, комментарии всё также на английском языке, но код стайл соблюдается

Задача 3 (ИСпользую  DeepSeek т. к. в Claude закончились бесплатные запросы)
Промт:

Напиши код для данной задачи на языке программирования на C++

Дано описание неориентированного графа в текстовом файле с именем FileName1. в виде 
матрицы смежности. Первая строка файла содержит количество вершин графа (n), а следующие 
n строк содержат матрицу смежности (a), a[i][j]=0, если ребра между вершинами i и j не суще
ствует. Построить матрицу инцидентности данного графа и вывести ее в файл с именем 
FileName2. Для справки: матрица инцидентности (b) имеет размер n x m, m - число ребер графа, 
b[i][j]=1, если ребро j инцидентно вершине i, в противном случае b[i][j]=0. Нумерацию ребер осу
ществлять в следующем порядке: сначала ребра, инцидентные вершине номер 1, потом ребра ин
цидентные вершине номер 2 и т.д. до вершины номер n. Ребра, инцидентные вершине с номером 
i перечислять в порядке возрастания номера второй вершины, инцидентной данному ребру. При 
выводе в первой строке указать размер матрицы инцидентности: числа n и m, а в следующих n 
строках разместить матрицу инцидентности.

Код стайл должен быть от epic games
Также нужно сделать следующее:
1) Разделить файлы на модули (2 файла формата .сpp, 1 файл формата .h)
2) Реализовать валидацию данных
3) Добавь комментарии на русском языке

Вывод: Рабочий код, однако он полностью не соответствует код стайлу от Epic Games. Пришлось править код самостоятельно.

Общий вывод по нейронке: Claude хорошо справляется с генераией при хорошем промте, но её решение в корне отличается от Gemini и ChatGPT.











